/**
 * Tiers are edited inside the designer's workspace (TemplateWorkspace saves
 * them through PATCH /programs/:id/tiers), so this tab shows that same view
 * rather than a second, divergent tier editor.
 */
export { default } from '../../../template-designer/view';
