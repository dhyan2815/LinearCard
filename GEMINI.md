# LinearCard Project Memory

## Overview
LinearCard generates digital passes for Google Wallet (and Apple Wallet) using service account credentials and signed JWTs.

## Current Architecture
- **Language/Runtime**: Node.js (ES Modules), Next.js 16 (App Router), React 19
- **Design System**: Linear-inspired dark mode theme, glassmorphism, responsive 3D card tilt & live preview, `lucide-react`, `qrcode.react`, `canvas-confetti`
- **Backend API**: [`/api/generate-pass`](file:///c:/Users/dhyan/Desktop/LinearCard/app/api/generate-pass/route.js) for on-the-fly RS256 JWT pass creation and Google Wallet link generation.
- **Google Wallet Integration**: Issues signed JWT `savetowallet` links containing `genericClasses` and `genericObjects` that directly open and save passes to Google Wallet on Android / web.

## Changelog
- **2026-08-26**: Fixed `SyntaxError: The requested module '@passmint/node' does not provide an export named 'GoogleSigningMaterial'`. Replaced outdated/hallucinated `@passmint/node` direct-signing calls with standard Google Wallet JWT signing using `jsonwebtoken` and Node `crypto`. Added `qrcode-terminal` for easy camera scanning of generated Google Wallet pass URLs.
- **2026-08-26**: Documented `@passmint/node` Passmint SaaS SDK architecture and usage guide. Passmint requires a Passmint API key (`PASSMINT_API_KEY`) and template ID (`templateId`) to issue passes via `passmint.passes.create()`, which populates `pass.google_wallet_url` and `pass.url`.
- **2026-08-26**: Fixed terminal QR code wrapping/distortion issues (caused by long 1,100+ character JWTs in narrow console windows). Replaced terminal ASCII QR with auto-generated `save-pass.html` containing a crisp high-res QR code, pass details card, and direct "Add to Google Wallet" button that automatically pops up in the default browser.
- **2026-08-26**: Removed 3rd-party `tinyurl` dependency. [`index.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/index.mjs) now outputs the direct official Google Wallet URL (`https://pay.google.com/gp/v/save/...`) exclusively and renders the high-res QR code and Save button locally via [`save-pass.html`](file:///c:/Users/dhyan/Desktop/LinearCard/save-pass.html).
- **2026-08-26**: Extracted [`generateHtmlPreview()`](file:///c:/Users/dhyan/Desktop/LinearCard/preview.mjs) into a dedicated module [`preview.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/preview.mjs) and imported it into [`index.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/index.mjs) for clean code modularity.
- **2026-08-26**: Converted the project into a complete Next.js App Router web application:
  - Created [`lib/google-wallet.js`](file:///c:/Users/dhyan/Desktop/LinearCard/lib/google-wallet.js) for modular cryptographic pass generation.
  - Created [`app/api/generate-pass/route.js`](file:///c:/Users/dhyan/Desktop/LinearCard/app/api/generate-pass/route.js) API route for live pass signing.
  - Built interactive Linear-style dark UI with live 3D preview ([`components/PassPreviewCard.jsx`](file:///c:/Users/dhyan/Desktop/LinearCard/components/PassPreviewCard.jsx)), preset templates, color swatches, celebratory modal ([`components/WalletModal.jsx`](file:///c:/Users/dhyan/Desktop/LinearCard/components/WalletModal.jsx)), high-res QR code, and session history in [`app/page.js`](file:///c:/Users/dhyan/Desktop/LinearCard/app/page.js).
  - Configured **Tailwind CSS v4** via [`postcss.config.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/postcss.config.mjs) and [`app/globals.css`](file:///c:/Users/dhyan/Desktop/LinearCard/app/globals.css) with custom glassmorphism and metallic shine animations.
  - Overhauled layout structure in [`app/page.js`](file:///c:/Users/dhyan/Desktop/LinearCard/app/page.js), [`components/PassPreviewCard.jsx`](file:///c:/Users/dhyan/Desktop/LinearCard/components/PassPreviewCard.jsx), and [`app/globals.css`](file:///c:/Users/dhyan/Desktop/LinearCard/app/globals.css) to fix overflow alignment, padding, and responsive grid balance.
  - Updated [`jsconfig.json`](file:///c:/Users/dhyan/Desktop/LinearCard/jsconfig.json) with Next.js JSX compiler options (`jsx: "preserve"`, `moduleResolution: "bundler"`) to resolve editor type checking and JSX parsing errors in [`app/page.js`](file:///c:/Users/dhyan/Desktop/LinearCard/app/page.js).
- **2026-08-26**: Created [`update-pass.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/update-pass.mjs) script to prototype live pass updates via the Google Wallet REST API. Used `jsonwebtoken` to generate a self-signed JWT and exchange it for a short-lived OAuth 2.0 access token to successfully `PATCH` the points balance on an existing saved pass.
- **2026-08-26**: Refactored [`update-pass.mjs`](file:///c:/Users/dhyan/Desktop/LinearCard/update-pass.mjs) to use the official `google-auth-library` package for authentication and dynamically read the `PASS_ID` from the `.env` file instead of hardcoding it.
- **2026-08-26**: Major architectural update: Migrated all CLI script operations (`index.mjs`, `update-pass.mjs`) into the unified Next.js dashboard UI. Added three tabs in `app/page.tsx`: 1) **Design Template** (pushes `GenericClass` templates to Google Wallet API), 2) **Issue Passes** (generates offline JWTs), and 3) **Manage Live** (instantly `PATCH`es live generic objects with new balances). Re-implemented `google-wallet.ts` with explicit `createGenericClass` and `updateGenericObject` functions.
- **2026-08-26**: Fixed text selection legibility issue across all input fields (including the "Program Title" input) by applying a high-contrast global `::selection` style using the brand orange color in `app/globals.css`.
- **2026-08-26**: Completed Phase 5 (Identity & Onboarding Web Flow) by refactoring `app/page.tsx` to include top-level state management (`currentScreen`). Built a new, informative landing experience and a step-by-step customer onboarding flow (Phone Number -> OTP & DPDP Consent -> Pass Issuance). The old backend tools were preserved and moved behind the `dashboard` view state. Mocked the OTP generation/validation with a realistic UI loading state until Phase 6 WhatsApp API is integrated.
- **2026-08-27**: Created mega end-to-end PoC implementation plan. Audited all 5 completed phases vs 8 identified gaps (G1-G8). Raised 4 open architectural questions (WhatsApp provider, scanner approach, transactional receipt, pass preview style). Proposed 5-phase execution order (A: critical fixes → B: WhatsApp → C: Scanner → D: Polish → E: Testing). Defined full test validation suite specification including `smoke-test.mjs` and `e2e-checklist.md`.
- **2026-08-27**: Installed and configured the `superpowers` plugin from `https://github.com/obra/superpowers` into the global Antigravity plugins directory (`~/.gemini/config/plugins/superpowers`), enabling 14 developer skills (brainstorming, TDD, systematic debugging, subagent-driven development, plan execution, code review, etc.).
- **2026-08-29**: Conducted a full codebase audit and generated [`LinearCard_Architecture_Briefing.md`](file:///c:/Users/dhyan/Desktop/LinearCard/LinearCard_Architecture_Briefing.md) in the project root. This 35KB document covers: complete tech stack, annotated file/directory structure, how all 5 routes and 7 API routes work, all 3 lib modules (google-wallet.ts, whatsapp.ts, db.ts), design system tokens, 10 critical gaps (broken enrollment flow, mocked auth, in-memory OTP store, no real DB, no camera scanner, no IAB Intent URI, orphaned files, dead dependency), and a full recommended target architecture (DB schema, auth architecture, async queue, component hierarchy, and priority-ranked restructure checklist).
- **2026-08-29**: Executed the Demo PoC Implementation Plan: Switched from in-memory to SQLite database via Prisma. Seeded database with multi-tenant mock data (Coffee & Gym). Implemented full backend API integration for consumer /api/send-otp and /api/verify-otp routes. Refactored the dashboard to be tenant-aware. Created an admin-only protected route behind middleware using HTTP-only JWTs via /api/admin/verify-otp. Created a live-camera QR scanner page (dark mode). Rewrote /api/validate-pass and /api/update-pass to use SQLite as a persistent source of truth and guard against duplicate updates. Added Android Intent URI deep linking to directly pop up the Wallet app from the success screen.
- **2026-08-30**: Created [`LinearCard_User_Flows.md`](file:///c:/Users/dhyan/Desktop/LinearCard/LinearCard_User_Flows.md) — comprehensive user flow document covering three personas (Brand Admin, Frontline Staff, End Member) with 11 detailed flows mapping every feature to actual API routes and frontend files, including a cross-persona summary matrix and known-gaps table.
- **2026-08-31**: Investigated a perceived gap in dashboard route protection. Discovered that the custom Next.js architecture deprecates `middleware.ts` in favor of `proxy.ts`, which was already correctly implemented and guarding the `/dashboard` and `/scan` routes. Deleted the conflicting `middleware.ts` file to fix the `npm run dev` server crash.
- **2026-08-31**: Refined the landing page messaging and updated global SEO metadata in `app/layout.tsx` to precisely align with the product positioning in `LinearCard_PRD_v1.md`.
- **2026-08-31**: Implemented a global dark/light mode toggle. Extracted the toggle logic into `components/ThemeToggle.tsx` and embedded it natively into `app/layout.tsx`.
- **2026-08-31**: Executed a codebase-wide styling migration across the `app/` and `components/` directories. Replaced hardcoded tailwind utility classes (e.g., `bg-zinc-950`, `border-zinc-800`, `text-white`) with the dynamic semantic color system (`bg-canvas`, `bg-surface-card`, `border-border-subtle`, `text-ink-dark`) to allow the UI to respond to the theme toggle.
- **2026-08-31**: Resolved a series of light mode edge-case bugs caused by the styling migration: restored the camera flip icon in `app/scan/page.tsx`, fixed invisible active tab text in `app/dashboard/page.tsx`, corrected the native OS `Select Tenant` dropdown by enforcing `color-scheme: light` in `globals.css`, and pinned the 3D phone's dynamic island notch back to a hardcoded black to match the physical mockup frame.
- **2026-08-31**: Created comprehensive 8-flow manual testing guide (`LinearCard_Manual_Testing_Guide.md`) covering pre-flight checks, customer enrollment with real WhatsApp OTP, admin login with JWT auth, template designer, pass issuance, live balance updates, staff QR scanner, and edge cases. Flagged 3 ambiguities with `[VERIFY IN CODE]` notes: Intent URI propagation in WalletModal, WhatsApp-delivered link lacking Intent rewrite from verify-otp, and pass ID format matching in the scanner page.
- **2026-08-31**: Fixed a double header bug on the `/enroll/[slug]` pages by removing the redundant `app/enroll/layout.tsx` which was wrapping the nested `app/enroll/[slug]/layout.tsx`.
- **2026-09-01**: Deleted the `prisma` directory and verified there are no remaining traces of Prisma or `dev.db` in the codebase since the migration to Supabase is complete.
- **2026-09-01**: Fixed 404 errors for tenant images (`hero-beanhouse.png`, `logo-beanhouse.png`, etc.) on the dashboard. Generated local placeholder images in the `public` directory, updated `setup.sql` and `app/api/tenant/[slug]/route.ts` to use relative URLs, and updated `app/api/generate-pass/route.ts` and `app/api/create-class/route.ts` to resolve these relative URLs to absolute ones (using `request.nextUrl.origin`) before sending them to the Google Wallet API, which strictly requires absolute URLs.
- **2026-09-01**: Reduced the heavy `shadow-xl shadow-black/50` on the `/dashboard` page `Card` components to a cleaner `shadow-sm` to improve the light mode aesthetic.
- **2026-09-02**: Redesigned the consumer enrollment success interface (`app/enroll/[slug]/page.tsx`) specifically for mobile devices. Replaced the desktop-style `WalletModal` overlay (which redundantly showed a QR code) with a sleek, inline responsive card featuring automatic confetti, clear typography, and a seamlessly embedded "Add to Google Wallet" button perfectly optimized to fit mobile viewports.
- **2026-09-02**: Implemented Task 1: OTP Security Hardening (PRD §7.3, §8). Added `lib/otp.ts` exporting SHA-256 `hashOtp`, timing-safe `verifyOtp`, and DB-driven `isOtpRateLimited` checking active unconsumed sessions within expiration. Updated consumer `/api/send-otp` and admin `/api/admin/send-otp` to hash OTPs before DB insertion and reject rapid duplicate requests with 429. Updated consumer `/api/verify-otp` and admin `/api/admin/verify-otp` to verify OTPs using constant-time comparison against stored SHA-256 hashes. Added `tests/otp-security.mjs` test suite.
- **2026-09-02**: Implemented Task 2: Supabase Schema Migrations (PRD §7.1, §7.4). Created SQL migrations in `supabase/migrations/` (`001_pass_template.sql`, `002_audit_log.sql`, `003_notification_log.sql`, `004_tenant_webhook.sql`) and synced `supabase/setup.sql`. Added `tests/schema-check.mjs` to verify presence of `PassTemplate`, `AuditLog`, `NotificationLog` tables and `Tenant.webhookUrl` column.
- **2026-09-02**: Implemented Task 3: PassTemplate CRUD & Publish API (PRD §7.2). Created `/api/templates` (GET by tenantId, POST with draft status & archetype validation), `/api/templates/[id]` (GET, PATCH, DELETE), and `/api/templates/[id]/publish` (syncs `GenericClass` with Google Wallet API, transitions template status to `published` and stores `googleClassId`). Added comprehensive test suite `tests/templates-api.mjs`.
- **2026-09-02**: Implemented Task 4: Template Builder UI (PRD §7.2). Updated `app/dashboard/page.tsx` with 4 pass archetypes (`loyalty`, `membership`, `id_card`, `access_badge`), draft/publish status indicator badge, and a two-button save flow ("Save as Draft" using `POST /api/templates` / `PATCH /api/templates/[id]`, "Publish to Wallet" using `POST /api/templates/[id]/publish`). Added tab switch & tenant selection auto-fetch for existing templates.
- **2026-09-02**: Implemented Task 5: Notification Delivery Ledger (PRD §7.5, §8). Created `lib/notify.ts` with non-blocking `logNotification` for `NotificationLog`. Appended `sendPassLinkWithLog` and `sendRedemptionReceiptWithLog` to `lib/whatsapp.ts`. Updated `/api/update-pass`, `/api/verify-otp`, and `/api/generate-pass` to log outbound notifications (WhatsApp and Wallet push notifications). Added test suite `tests/notify-ledger.mjs`.

