{
  "name": "linearcard-monorepo",
  "private": true,
  "packageManager": "npm@11.10.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run kill-ports && npm run build -w packages/types && turbo run dev",
    "dev:clean": "npm run kill-ports && npm run build -w packages/types && turbo run dev",
    "kill-ports": "npx -y kill-port 3000 3001 || true",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "supabase": "^2.117.0",
    "turbo": "latest",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "googleapis": "^180.0.0"
  }
}
